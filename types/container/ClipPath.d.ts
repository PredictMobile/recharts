import React from 'react';
import { ChartOffset } from '../util/types';
type ClipPathProps = {
    clipPathId: string;
    offset: ChartOffset;
};
export declare function ClipPath({ clipPathId, offset }: ClipPathProps): React.JSX.Element;
export {};
